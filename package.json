{
  "name": "prs-stat-cards",
  "version": "0.1.0",
  "description": "Generate GitHub PR Stats cards in GitHub Actions",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --runInBand",
    "format": "prettier --write .",
    "lint-staged": "lint-staged",
    "prepare": "husky install"
  },
  "dependencies": {
    "@actions/core": "^2.0.1",
    "@stats-organization/github-readme-stats-core": "^2.1.5"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "*.{js,json,md,yml,yaml}": "prettier --write"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
